// Intentionally empty.
//
// A `binaryTarget` cannot carry `linkerSettings`, so this source-only target exists solely
// to attach the system libraries the Kotlin/Native runtime needs to whatever links the SDK.
// Every product that vends a Wallet SDK framework also vends this target.
